<Window x:Class="LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QT/MR Tracker – logowanie"
        Height="220" Width="360"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Użytkownik"/>
        <TextBox x:Name="UsernameBox" Grid.Row="0" Margin="0,20,0,0"/>

        <TextBlock Text="Hasło" Grid.Row="1" Margin="0,12,0,0"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="0,32,0,0"/>

        <Button Content="Zaloguj"
                Grid.Row="2"
                Height="34"
                Margin="0,16,0,0"
                Click="Login_Click"/>

        <TextBlock x:Name="ErrorText" Grid.Row="3" Foreground="Tomato" Margin="0,8,0,0"/>
    </Grid>
</Window>
